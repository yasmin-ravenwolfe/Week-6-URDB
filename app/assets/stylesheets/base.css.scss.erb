body {
  background-image: url(<%= asset_path('wavegrid.png') %>);
}

.main {
  padding-top: 40px;
  @include grid-row;
}

.title {
  @include grid-column(6);
}

.actions {
  @include grid-column(6);
  padding-top: 15px;
}

a.button:visited {
  color: white;
}