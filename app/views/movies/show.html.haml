#poster
  %img{src: @movie.poster_url.to_s}

#movie
  #info
    #title
      %h1= @movie.title
    #description
      %p= @movie.description
    #trailer
      %i.icon-youtube-play
      =link_to "Watch the trailer on YouTube", @movie.trailer_url, {target: "blank"}

  #showings
    %h2.centered Showings

    %table
      %thead
        %th Location
        %th Time
      %tbody
      - @showings.each do |showing|
        %tr
          %td= showing.location
          %td= showing.time.to_s(:simple)

    = simple_form_for [@movie, @showing] do |f|
      .location= f.input :location
      .time= f.input :time
      .submit= f.button :submit