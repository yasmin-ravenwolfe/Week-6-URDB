%h1
  %i.icon-film
  Movies

%table
  %tr
    %th Title
    %th
    %th

  - @movies.each do |movie|
    %tr
      %td= link_to movie.title, movie
      %td.centered= link_to '<i class="icon-edit"></i>'.html_safe, edit_movie_path(movie)
      %td.centered= link_to '<i class="icon-trash"></i>'.html_safe, movie, :method => :delete, :data => { :confirm => 'Are you sure you want to delete this movie?' }

%br

= link_to 'New Movie', new_movie_path
